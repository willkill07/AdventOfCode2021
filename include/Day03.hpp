#pragma once

#include <bitset>
#include <list>
#include <string>

#include <scn/all.h>

namespace day03 {
  constexpr const int id = 3;
  using parsed = std::vector<std::bitset<16>>;
  using result1 = int;
  using result2 = int;
}
